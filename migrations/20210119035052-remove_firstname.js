'use strict';

module.exports = {
  up: async (queryInterface, Sequelize) => {
    // await queryInterface.removeColumn('Users', 'firstName', 'lastName');
  },

  down: async (queryInterface, Sequelize) => {
    // await queryInterface.addColumn('Users', 'firstName', 'lastName');
  }
};
